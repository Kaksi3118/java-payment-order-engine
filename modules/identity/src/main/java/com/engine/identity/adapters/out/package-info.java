/**
 * Driven adapters &mdash; implement the domain's outbound ports against infrastructure (DB, JWT,
 * password hashing, outbox). Each adapter is a leaf of the dependency graph.
 */
package com.engine.identity.adapters.out;